import React, { Component, Fragment } from 'react';
//import Animate from 'animated';

class StatueEditor extends Component {
    render() {
        return (
            <Fragment>
                
            </Fragment>
        );
    }
}

export default StatueEditor;
//export default Animate.createAnimatedComponent(StatueEditor);