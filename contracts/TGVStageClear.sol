pragma solidity ^0.4.22;

import "./TGVItemShop.sol";

contract TGVStageClear is TGVItemShop {

    // 진행하고자 하는 stage 값
    // user 석상 갯수
    // 각 석상의 능력치 정보
    //stage에 해당 하는 빌런 정보


        //석상 레벨 별 능력치 계산기
        //uint k = 2;
        //uint mylevel = user[msg.sender].level;
        //statueInfoList[1].hp.add(mylevel*3*k);
        //statueInfoList[1].atk.add(mylevel*2*k);
        //statueInfoList[1].def.add(mylevel*k);
        
    



}